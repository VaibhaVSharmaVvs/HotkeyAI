{
  "schemaVersion": 1,
  "name": "Play / Pause",
  "description": "Toggles playback in whichever application currently owns media playback. Works when that application is minimized or on another desktop.",
  "trigger": {
    "type": "hotkey",
    "keys": ["CTRL", "ALT", "S"]
  },
  "actions": [
    {
      "id": "s1",
      "type": "send_appcommand",
      "comment": "Broadcast the media key rather than finding and focusing a player window. Playback state is not observable, so this action is deliberately unverified.",
      "command": "media_play_pause"
    }
  ]
}
